<div class='well'>
    <h4>The Universe is the totality of everything that exists, has existed, or ever will exist.</h4>
    <p>This Q&amp site is dedicated to questions about the history of the Universe</p>
</div>


<div class = 'container'>
  <% @questions.each do |q| %>
    <div class="media">
      <div class='media-left'>
        <a href='#'>
          <img class="media-object" src="<%= q.gravatar %>" style="width:50px" >
        </a>
      </div>
      <div class="media-body">
        <h5 class="mt-0"><%= q.email %> asked:</h5>
        <div><%= time_ago_in_words q.created_at %> ago</div>
        <%= q.body %>
      </div>
      <div>
        <a href='<%= question_path(q) %>' class='btn btn-success btn-xs'>View Answer</a>
      </div>
    </div>
  <% end %>
</div>